Ah, también hay que tipar las _variables locales_, que se ven igualitas a los atributos:

```java 
void unCalculoComplejo() {
  int unaVariableLocal = ...;
  int otraVariableLocal = ...;
  ...
}
```

¿Y qué hay del `void`?